enum SendType {
  CONTACT,
  BUSINESS,
  QR_ADDRESS,
  FUSE_ADDRESS,
  PASTED_ADDRESS,
  ETHEREUM_ADDRESS,
}

enum BiometricAuth {
  faceID,
  touchID,
  pincode,
  none,
}

enum OnboardStatergy {
  firebase,
  sms,
  none,
}
